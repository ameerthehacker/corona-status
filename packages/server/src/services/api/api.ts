export function getTotalDeaths(country: string): number {
  return 0;
}

export function getNewCases(country: string): number {
  return 0;
}

export function getTotalRecovered(country: string): number {
  return 0;
}
